<h1><%=@portfolio_item.title.to_s %></h1>

<%= image_tag@portfolio_item.main_image.to_s %>
<%=@portfolio_item.body.to_s %>

<br>
<%= link_to 'Edit',  edit_portfolio_path(@portfolio_item.id) %>
<%= link_to 'Back', portfolios_path %>
<%= link_to 'Delete Portfolio Item',@portfolio_item, method: :delete, data: { confirm: 'Are you Sure?' } %>

 <% unless@portfolio_item.technologies.empty? %>
	<h2>Technologies Used</h2>
	<% @portfolio_item.technologies.each do |t| %>
		<%= t.name %>
	<% end %>
<% end %>

<hr>

<%= params %>

<%# <hr> %>
<%#= session.inspect %>
