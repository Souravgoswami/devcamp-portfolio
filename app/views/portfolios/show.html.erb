<h1><%= @item.title.to_s %></h1>

<%= image_tag @item.main_image.to_s %>
<%= @item.body.to_s %>

<br>
<%= link_to 'Edit',  edit_portfolio_path(@item.id) %>
<%= link_to 'Back', portfolios_path %>
<%= link_to 'Delete Portfolio Item', @item, method: :delete, data: { confirm: 'Are you Sure?' } %>

 <% unless @item.technologies.empty? %>
	<h2>Technologies Used</h2>
	<% @item.technologies.each do |t| %>
		<%= t.name %>
	<% end %>
<% end %>

<hr>

<%= params %>

<%# <hr> %>
<%#= session.inspect %>
